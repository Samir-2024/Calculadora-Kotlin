<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base do tema claro -->
    <style name="Base.Theme.Calculadora" parent="Theme.Material3.DayNight.NoActionBar" />

    <!-- Tema personalizado claro -->
    <style name="Theme.MyCalculator.Light" parent="Base.Theme.Calculadora">
        <item name="colorPrimary">@color/azul</item>
        <item name="colorPrimaryContainer">@color/azul_claro</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:windowBackground">@color/white</item>
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/black</item>
    </style>

    <!-- Tema principal claro (pode mudar em runtime) -->
    <style name="Theme.Calculadora" parent="Theme.MyCalculator.Light" />
</resources>
